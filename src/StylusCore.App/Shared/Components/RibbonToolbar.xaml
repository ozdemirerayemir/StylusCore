<UserControl x:Class="StylusCore.App.Shared.Components.RibbonToolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.AppBg}">

    <UserControl.Resources>
        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSize_Body}"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Brush.Surface}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Tabs (always visible) -->
            <RowDefinition Height="36"/>
            <!-- Content (collapsible) -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab Headers -->
        <Border Grid.Row="0"
                BorderBrush="{DynamicResource Brush.BorderSoft}"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="8,0">
                <RadioButton x:Name="FileTab"
                             Content="{DynamicResource Str_Ribbon_File}"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
                <RadioButton x:Name="EditTab"
                             Content="{DynamicResource Str_Ribbon_Edit}"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
                <RadioButton x:Name="ViewTab"
                             Content="{DynamicResource Str_Ribbon_View}"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
                <RadioButton x:Name="ToolsTab"
                             Content="{DynamicResource Str_Ribbon_Tools}"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
            </StackPanel>
        </Border>

        <!-- Tab Content (Collapsible Panel) -->
        <Border x:Name="ContentPanel"
                Grid.Row="1"
                Height="0"
                BorderBrush="{DynamicResource Brush.BorderSoft}"
                BorderThickness="0,0,0,1"
                ClipToBounds="True">

            <!-- File Tab Content -->
            <StackPanel x:Name="FileTabContent"
                        Orientation="Horizontal"
                        Margin="16,8"
                        VerticalAlignment="Top">
                <StackPanel Margin="0,0,24,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="{DynamicResource ControlHeightLG}" Height="{DynamicResource ControlHeightLG}" ToolTip="{DynamicResource Str_New}">
                        <TextBlock Text="📄" FontSize="{DynamicResource FontSize_Header}"/>
                    </Button>
                    <TextBlock Text="{DynamicResource Str_New}" FontSize="{DynamicResource FontSize_Small}" HorizontalAlignment="Center"
                               Foreground="{DynamicResource Brush.TextSecondary}"/>
                </StackPanel>

                <StackPanel Margin="0,0,24,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="{DynamicResource ControlHeightLG}" Height="{DynamicResource ControlHeightLG}" ToolTip="{DynamicResource Str_Open}">
                        <TextBlock Text="📂" FontSize="{DynamicResource FontSize_Header}"/>
                    </Button>
                    <TextBlock Text="{DynamicResource Str_Open}" FontSize="{DynamicResource FontSize_Small}" HorizontalAlignment="Center"
                               Foreground="{DynamicResource Brush.TextSecondary}"/>
                </StackPanel>

                <StackPanel Margin="0,0,24,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="{DynamicResource ControlHeightLG}" Height="{DynamicResource ControlHeightLG}" ToolTip="{DynamicResource Str_Save}">
                        <TextBlock Text="💾" FontSize="{DynamicResource FontSize_Header}"/>
                    </Button>
                    <TextBlock Text="{DynamicResource Str_Save}" FontSize="{DynamicResource FontSize_Small}" HorizontalAlignment="Center"
                               Foreground="{DynamicResource Brush.TextSecondary}"/>
                </StackPanel>

                <Border Width="1" Background="{DynamicResource Brush.BorderSoft}" Margin="8,4"/>

                <StackPanel Margin="24,0,0,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="{DynamicResource ControlHeightLG}" Height="{DynamicResource ControlHeightLG}" ToolTip="{DynamicResource Str_ExportPDF}">
                        <TextBlock Text="📑" FontSize="{DynamicResource FontSize_Header}"/>
                    </Button>
                    <TextBlock Text="{DynamicResource Str_Export}" FontSize="{DynamicResource FontSize_Small}" HorizontalAlignment="Center"
                               Foreground="{DynamicResource Brush.TextSecondary}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
