<UserControl x:Class="StylusCore.App.Controls.RibbonToolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource PrimaryBackgroundBrush}">

    <UserControl.Resources>
        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource TertiaryBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryAccentBrush}"/>
                                <Setter TargetName="Border" Property="BorderBrush"
                                        Value="{DynamicResource PrimaryAccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Tabs (always visible) -->
            <RowDefinition Height="36"/>
            <!-- Content (collapsible) -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab Headers -->
        <Border Grid.Row="0"
                BorderBrush="{DynamicResource PrimaryBorderBrush}"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="8,0">
                <RadioButton x:Name="FileTab"
                             Content="File"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
                <RadioButton x:Name="EditTab"
                             Content="Edit"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
                <RadioButton x:Name="ViewTab"
                             Content="View"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
                <RadioButton x:Name="ToolsTab"
                             Content="Tools"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked"
                             Click="Tab_Click"/>
            </StackPanel>
        </Border>

        <!-- Tab Content (Collapsible Panel) -->
        <Border x:Name="ContentPanel"
                Grid.Row="1"
                Height="0"
                BorderBrush="{DynamicResource PrimaryBorderBrush}"
                BorderThickness="0,0,0,1"
                ClipToBounds="True">

            <!-- File Tab Content -->
            <StackPanel x:Name="FileTabContent"
                        Orientation="Horizontal"
                        Margin="16,8"
                        VerticalAlignment="Top">
                <StackPanel Margin="0,0,24,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="48" Height="48" ToolTip="New">
                        <TextBlock Text="📄" FontSize="20"/>
                    </Button>
                    <TextBlock Text="New" FontSize="10" HorizontalAlignment="Center"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>

                <StackPanel Margin="0,0,24,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="48" Height="48" ToolTip="Open">
                        <TextBlock Text="📂" FontSize="20"/>
                    </Button>
                    <TextBlock Text="Open" FontSize="10" HorizontalAlignment="Center"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>

                <StackPanel Margin="0,0,24,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="48" Height="48" ToolTip="Save">
                        <TextBlock Text="💾" FontSize="20"/>
                    </Button>
                    <TextBlock Text="Save" FontSize="10" HorizontalAlignment="Center"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>

                <Border Width="1" Background="{DynamicResource PrimaryBorderBrush}" Margin="8,4"/>

                <StackPanel Margin="24,0,0,0">
                    <Button Style="{DynamicResource IconButtonStyle}" Width="48" Height="48" ToolTip="Export PDF">
                        <TextBlock Text="📑" FontSize="20"/>
                    </Button>
                    <TextBlock Text="Export" FontSize="10" HorizontalAlignment="Center"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
